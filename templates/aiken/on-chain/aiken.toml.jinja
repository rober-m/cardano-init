name = "{{ project_name }}/on-chain"
version = "0.0.0"
license = "Apache-2.0"
description = "On-chain validators for {{ project_name }}"

[repository]
user = "{{ project_name }}"
project = "on-chain"
platform = "github"

[[dependencies]]
name = "aiken-lang/stdlib"
version = "v2"
source = "github"
